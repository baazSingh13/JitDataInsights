{
  "name": "jitdatainsights-website",
  "version": "1.0.0",
  "private": true,
  "description": "Static JitDataInsights company website",
  "scripts": {
    "start": "node tools/serve.cjs",
    "build": "node tools/build.cjs",
    "test": "node --test tests/*.test.cjs",
    "check": "node --check script.js && node --check neural_sync.js && npm test && npm run build"
  },
  "engines": { "node": "^22.22.2 || ^24.15.0 || >=26.0.0" },
  "devDependencies": { "jsdom": "30.0.1" }
}
